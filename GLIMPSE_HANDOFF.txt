===============================================================
GLIMPSE — PROJECT HANDOFF DOC
===============================================================

----------------------------------------------------------------
WORKFLOW
----------------------------------------------------------------
This project is built collaboratively between Claude (claude.ai)
and Claude Code (CC) running in the terminal. The workflow is:

- Claude (claude.ai chat) acts as creative director and technical
  translator. The human gives feedback and direction in plain
  language. Claude interprets it and writes precise prompts for CC.

- Claude Code (terminal) executes all file edits directly.
  It reads and writes index.html, style.css, and main.js.

- The human never writes code directly. They describe what they
  want, Claude drafts the CC prompt, human pastes it into CC.

- Screenshots are shared in the Claude chat when visual feedback
  is needed. Claude reads them and adjusts the next CC prompt.

- The Claude chat is for strategy, feedback, and prompt generation
  only. CC is for execution.


----------------------------------------------------------------
SESSION STARTUP — PASTE INTO CC AT THE START OF EVERY SESSION
----------------------------------------------------------------

  We are resuming work on the Glimpse portfolio site.
  The project lives at: C:/Users/Trader/Desktop/glimpse

  Start the dev server:
  npm run dev

  Then read the current state of these files and summarize:
  - index.html
  - style.css
  - main.js
  - elladj.html
  - vite.config.js

  Do not make any changes yet. Just confirm the server is running
  and report the current state of the files.


----------------------------------------------------------------
SESSION STARTUP — PASTE INTO CLAUDE.AI AT THE START OF EVERY SESSION
----------------------------------------------------------------

  We are resuming work on the Glimpse portfolio website.
  I am going to paste in our handoff doc so you have full context.
  Please read it and confirm you understand the project, the
  workflow, and where we left off. Do not suggest any changes yet.
  Wait for my first piece of feedback or direction.

  [paste this entire document below that message]


----------------------------------------------------------------
PROJECT OVERVIEW
----------------------------------------------------------------
Client:         Glimpse — a creative film studio
Site type:      Portfolio / showcase
Stack:          Vite + vanilla JS, single page, no framework
Dev server:     npm run dev → http://localhost:5173
Build command:  npm run build → outputs to dist/
Deployment:     Vercel, auto-deploys on every git push
To deploy:      git add . → git commit -m "message" → git push
Repo:           https://github.com/glimpsefilms/GlimpseSite.git
Vercel project: glimpsefilms account


----------------------------------------------------------------
FILE STRUCTURE
----------------------------------------------------------------

  glimpse/
  ├── index.html           ← homepage markup
  ├── elladj.html          ← Elladj project page (real copy + credits)
  ├── jasmine.html         ← Jasmine project page (placeholder)
  ├── momo.html            ← Momo project page (placeholder)
  ├── shu.html             ← Shu project page (placeholder)
  ├── sondre.html          ← Sondre project page (placeholder)
  ├── annika.html          ← Annika project page (placeholder)
  ├── style.css            ← all styles
  ├── main.js              ← all JS (imports style.css)
  ├── vite.config.js       ← multi-page build config (lists all HTML inputs)
  ├── vercel.json          ← cleanUrls + outputDirectory: "dist"
  ├── package.json         ← vite + lenis dependencies
  ├── .gitignore           ← excludes node_modules, dist, .env
  ├── public/
  │   └── assets/
  │       └── Vector1.svg       ← Momo figure outline (unused for now)
  ├── node_modules/
  └── glimpse.html         ← original single-file reference (do not touch)


----------------------------------------------------------------
DESIGN TOKENS
----------------------------------------------------------------

  --bg:        #d4d4d4   (page background — warm grey)
  --ink:       #0a0a0a   (headline black)
  --graphite:  #4a4a4a   (subhead / body copy)
  --mid:       #6e6e6e   (labels, metadata, muted text)

  Fonts:
  - Headlines:  Montserrat 800 — uppercase
  - Body / UI:  Open Sans 300–600
  - Both loaded via Google Fonts


----------------------------------------------------------------
WHAT IS BUILT
----------------------------------------------------------------

HERO
- Fixed position, centered in viewport
- Headline (two-stanza, line-break separated):
    "We make documentary films
     driven by people, shaped by ambition."
  Montserrat 700, black, uppercase, centered
- font-size: clamp(28px, 4vw, 56px)
- No subhead
- Fades out as user scrolls
  Starts fading immediately, fully gone by 50% of viewport height
- #hero-text opacity driven by scroll listener in main.js
  (listener guarded with if (heroSection) so project pages don't throw)

NAVIGATION
- Fixed top — logo left, menu items right
- Logo: Glimpse-logo.gif (animated GIF) in nav, 32px tall
- Menu items stacked vertically, right-aligned
- Font: Open Sans, 16px, weight 600, uppercase, letter-spacing .14em
- Hover: letter-spacing expands to .64em, anchored right
  (transform-origin: right center)
- No underline on hover
- Items: WORK, INFO (Contact removed)

PROJECTS SECTION
- z-index: 3, background: transparent
  Hero text visible through gaps between cards
- margin-top: 75vh so first card peeks above fold on load
- padding-left: 80px to give room for vertical labels
- Layout: display flex, flex-wrap wrap, justify-content space-between
  This matches Mike Carson's grid system exactly
- Card sizing:
    Odd cards:   flex-basis 50%,  height 340px
    Even cards:  flex-basis 32%,  height 460px,
                 margin-top 200px, margin-left 200px
    4n+4 cards:  margin-top 200px
    4n+3 cards:  margin-top 0
- margin-bottom: 360px between cards for generous negative space
- Each card has a .proj-label — vertical text using
  writing-mode: vertical-rl, transform: scale(-1,-1),
  positioned left: -55px

CARDS
- .proj-thumb: overflow hidden, width 100%, height 100%
- .proj-thumb-inner: position absolute, inset 0
- Placeholder boxes: darker fill, image icon + label
- Card 1 (Elladj):  autoplay looping video /assets/ElladjPreview.mp4 (no still),
                    links to elladj.html
- Card 2 (Jasmine): real image /assets/JasmineSmall.jpg,
                    links to jasmine.html
- Card 3 (Momo):    real image /assets/MomoSmall.jpg + hover video /assets/MomoPreview.mp4,
                    links to momo.html
- Card 4 (Sondre):  placeholder, links to sondre.html
- Card 5 (Shu):     real image /assets/ShuSmall.jpg + hover video /assets/ShuPreview.mp4,
                    links to shu.html
- Card 6 (Annika):  placeholder, links to annika.html
- Hover: image scales 1.05, dark overlay fades in, hover video crossfades over still
- .proj-meta: project name + index number below card

GRAIN
- Tuned up: more present, denser texture
- #grain fixed overlay, z-index 2, opacity 0.12
- Animated SVG turbulence noise, shifts every 0.06s
- body::after second grain layer, opacity 0.07, offset phase

CANVAS BG
- #scrub-canvas fixed, z-index 0, opacity 0.22
- Animated ambient light pools that drift slowly
- Scroll position fed into animation phase (simulates video scrub)
- REPLACE with real <video> element when footage is ready

CUSTOM CURSOR
- Small dot, blooms to open ring on hover over interactive elements

SMOOTH SCROLL
- Lenis smooth scroll installed and wired into main.js
- duration 1.2, custom easing, drives the whole page

INFO / ABOUT (rebuilt)
- Full viewport height, black background, contact bar pinned at bottom
- Body copy stack: three paragraphs about Glimpse and founder
- Contact bar: STUDIO@GLIMPSE-FILMS.COM + YouTube + Instagram icons
- Covers hero as user scrolls past cards

PROJECT PAGES
- Six project pages exist: elladj.html, jasmine.html, momo.html, shu.html,
  sondre.html, annika.html. Each homepage card routes to its detail page
  via an inline onclick='window.location=...' on .proj-card.
- Each page is a Vite entry (see vite.config.js > rollupOptions.input)
  and is emitted to dist/ on `npm run build`.
- vercel.json sets cleanUrls: true so /elladj, /momo, etc. work without
  the .html suffix on the deployed site.

TEMPLATE STRUCTURE (every project page)
- .proj-page-wrap (flex container)
    .left-reel (sticky sidebar, desktop only)
      - Glimpse logo (links home)
      - "Info" link (links to /#about)
    .content-wrapper
      - Top "← BACK TO PROJECTS" link
      - <h1 class="proj-title">
      - <img class="proj-photo-main"> (or no src for placeholder block)
      - .proj-desc-block
          .proj-type     — directed/produced credit line
          p.proj-text    — body copy
      - dl.proj-credits  — role/name pairs in two-column grid
      - Bottom "← BACK TO PROJECTS" link

CONTENT STATUS
- elladj.html — REAL: title "Wild Ice: Elladj Baldé", real body copy,
  full 11-row credits block, image /assets/ElladjSmall.png
- jasmine.html, momo.html, shu.html — PLACEHOLDER: real images, lorem
  body copy, six generic credit rows ("Director / Name", etc.)
- sondre.html, annika.html — PLACEHOLDER: no image (renders 500px
  --card-bg block via .proj-photo-main:not([src]) rule), lorem copy,
  generic credits

RESPONSIVE
- Desktop (>1100px): two-column with sticky left-reel sidebar
- Tablet (≤1100px): left-reel hidden, single-column, padding 40px,
  credit rows tighten to 180px / 1fr
- Mobile (≤479px): same single-column, padding 16px, credit rows
  collapse to single column (role stacked above name), title scales
  down further

RESPONSIVE
- Tablet breakpoint: 768px — scaled stagger layout, cards still offset
  but tighter (flex-basis 55%/34%, smaller heights, reduced top margins)
- Mobile breakpoint: 430px — single column stack, all cards full width,
  vertical labels hidden
- Glassy frosted nav on mobile and tablet:
  background rgba(212,212,212,0.75) + backdrop-filter blur(8px),
  pinned top, items laid out in a row


----------------------------------------------------------------
WHAT IS NEXT (in priority order)
----------------------------------------------------------------

DONE
- Individual project pages — all 6 built (elladj, jasmine, momo, shu, sondre, annika).
  elladj.html has full real copy, full credits block, and real image.
  All other pages are placeholder templates awaiting client copy and images.
- All project pages registered in vite.config.js as build inputs.
- MP4 hover/autoplay previews — Elladj (autoplay looping), Momo (hover), Shu (hover).
- Vertical text labels on cards.
- Mobile + tablet responsive pass on project pages.

REMAINING (in priority order)
1. Real copy for remaining project pages (client to supply): Jasmine, Momo, Sondre, Shu, Annika

2. Remaining images: Sondre, Annika (real images and/or hero stills)

3. Optional hover videos: Jasmine, Sondre, Annika

4. Client upgrade Vercel to Pro for team access

5. Custom domain setup on Vercel

6. Final QA pass before launch


----------------------------------------------------------------
KEY RULES FOR CC — DO NOT VIOLATE THESE
----------------------------------------------------------------

- Always keep background: transparent on .projects-section
  and .projects-grid
- Never add background color to card wrappers (.proj-card,
  .proj-thumb) — backgrounds only go on .proj-placeholder elements
- Hero stays position: fixed — do not change to static or absolute
- All static assets go in public/assets/ — not root assets/
- Vite serves public/ at root — reference files as /assets/filename
- Do not touch glimpse.html — it is the original reference only
- Do not use CSS grid for the projects layout — it must stay flex


----------------------------------------------------------------
REFERENCE SITE
----------------------------------------------------------------
Mike Carson (mikecarson.tv) was used as the primary visual
reference for layout, card sizing, grid rhythm, and vertical
text labels. His grid uses the same flex wrapping system
described above.

----------------------------------------------------------------
CURRENT CARD SIZING RULES (style.css — exact values)
----------------------------------------------------------------

Base card rule:

  .proj-card {
    position: relative;
    cursor: none;
    margin-bottom: 50px;
  }

Base odd/even pattern:

  .proj-card:nth-child(odd) {
    flex-basis: 50%;
    height: 340px;
  }
  .proj-card:nth-child(even) {
    flex-basis: 32%;
    height: 460px;
    margin-top: 160px;
    margin-left: 160px;
  }

Row-2 margin overrides:

  .proj-card:nth-child(4n+4) {
    margin-top: 160px;
  }
  .proj-card:nth-child(4n+3) {
    margin-top: 0;
  }

Explicit per-card overrides (current DOM order):

  /* Card 1 — Elladj. Inherits odd's flex-basis: 50%, overrides height,
     resets margins so first card sits flush. */
  .proj-card:nth-child(1) {
    height: 338px;
    margin-top: 0;
    margin-left: 0;
  }

  /* Card 2 — Jasmine. Matches even pattern explicitly + adds bottom margin. */
  .proj-card:nth-child(2) {
    flex-basis: 32%;
    height: 460px;
    margin-top: 160px;
    margin-left: 160px;
    margin-bottom: 50px;
  }

  /* Card 5 — Shu. Inherits odd's flex-basis: 50%, overrides height
     to a shorter panoramic ratio. */
  .proj-card:nth-child(5) {
    height: 252px;
  }

DOM ORDER (index.html)
  1. Elladj   (data-index="1") — autoplay video,           links to elladj.html
  2. Jasmine  (data-index="2") — real image,               links to jasmine.html
  3. Momo     (data-index="3") — real image + hover video, links to momo.html
  4. Sondre   (data-index="4") — placeholder,              links to sondre.html
  5. Shu      (data-index="5") — real image + hover video, links to shu.html
  6. Annika   (data-index="6") — placeholder,              links to annika.html


===============================================================
END OF HANDOFF DOC
===============================================================
